# 📚 STM32 Проект - Объяснение для новичков

## 🎯 Что такое STM32?

**STM32** - это микроконтроллер (маленький компьютер на одной микросхеме) от компании STMicroelectronics.

### Ваша плата: BlackPill STM32F411CE

- **Процессор:** ARM Cortex-M4 @ 100 МГц
- **RAM:** 128 КБ (оперативная память)
- **Flash:** 512 КБ (память для программы)
- **Кварц:** 25 МГц (внешний генератор тактовой частоты)

Это как Arduino, но намного мощнее и сложнее в настройке.

---

## 📁 Структура проекта PlatformIO

```
STM32_Core/
├── src/                           # 📝 Ваш исходный код
│   ├── main.c                     # Главная программа
│   ├── main.h                     # Заголовочный файл для main.c (объявления функций, подключение HAL)
│   ├── stm32f4xx_it.c             # Обработчики прерываний
│   └── (другие .c файлы)
│
├── include/                       # 📋 Заголовочные файлы проекта
│   ├── stm32f4xx_hal_conf.h       # Конфигурация HAL библиотеки
│   ├── stm32f4xx_it.h             # Заголовки прерываний
│   └── (другие .h файлы)
│
├── lib/                           # 📚 Дополнительные библиотеки
│   └── (здесь можно добавлять свои библиотеки)
│
├── test/                          # 🧪 Файлы для тестирования
│
├── .pio/                          # ⚙️ Служебная папка PlatformIO
│   └── build/                     # Скомпилированные файлы (.o, .elf, .bin)
│
├── platformio.ini                 # ⚙️ Конфигурация проекта
└── learning/                      # 📖 Учебные материалы
    └── 01_project_structure.md    # Этот файл
```

### Что где хранится

- **src/** - весь ваш C/C++ код
- **include/** - заголовочные файлы (.h)
- **lib/** - сторонние библиотеки
- **.pio/build/** - результат компиляции (не редактируем вручную!)

---

## 🔧 Что такое HAL?

**HAL** = Hardware Abstraction Layer (слой абстракции от аппаратного обеспечения)

Это библиотека от STMicroelectronics, которая упрощает программирование STM32.

### 🚫 Без HAL (сложно)

```c
// Нужно знать точные адреса регистров памяти
#define RCC_BASE   0x40023800
#define GPIOC_BASE 0x40020800

*((uint32_t*)(RCC_BASE + 0x30)) |= 0x04;  // Включить GPIOC (GPIOC это 3-я битовая позиция)
*((uint32_t*)(GPIOC_BASE + 0x00)) |= (1 << 26); // Пин 13 на выход
*((uint32_t*)(GPIOC_BASE + 0x14)) ^= (1 << 13); // Переключить пин 13
```

### ✅ С HAL (просто и понятно)

```c
__HAL_RCC_GPIOC_CLK_ENABLE();           // Включить тактирование GPIOC
GPIO_InitTypeDef GPIO_InitStruct = {0}; // Настроить структуру для пина 13
GPIO_InitStruct.Pin = GPIO_PIN_13;      // Выбрать пин 13
GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP; // Режим: выход Push-Pull
HAL_GPIO_Init(GPIOC, &GPIO_InitStruct); // Настроить GPIO
HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13); // Переключить пин
```

**Преимущества HAL:**

- ✅ Проще читать и понимать
- ✅ Меньше ошибок
- ✅ Легко переносить код между STM32
- ✅ Официальная поддержка от ST

---

## 🔍 Проблема и её решение

### ❌ Что было сначала

```c
#include "main.h"  // ❌ Файл не существует!

int main(void) {
    HAL_Init();    // ❌ Компилятор не знает откуда эта функция
    ...
}
```

**Ошибка компиляции:**

```
fatal error: main.h: No such file or directory
```

### 🔎 Причины ошибки

1. Файл `main.h` не создан
2. Нет подключения к HAL библиотеке
3. Нет конфигурации HAL (`stm32f4xx_hal_conf.h`)
4. Нет обработчиков прерываний

---

## 🛠️ Решение: Созданные файлы

### 1️⃣ main.h - "Справочник" для main.c

**Расположение:** `src/main.h`

```c
#ifndef __MAIN_H // Защита от двойного включения
#define __MAIN_H // __MAIN_H - уникальный идентификатор для этого файла (может быть любым, главное чтобы был уникальным)

#ifdef __cplusplus // Если компилятор C++, используем C linkage для совместимости с C кодом
extern "C" { // Указываем, что функции имеют C linkage (не изменять имена при компиляции)
// не изменять имена функций (не использовать C++ name mangling)
#endif // Это нужно для совместимости с C++ кодом, если вы когда-нибудь будете использовать C++ в этом проекте

#include "stm32f4xx_hal.h"  // Подключить HAL библиотеку

void Error_Handler(void);   // Объявить функцию обработки ошибок, void означает, что функция не возвращает значение

#ifdef __cplusplus // Закрываем extern "C" блок для C++ компилятора
}
#endif

#endif /* __MAIN_H */
```

**Что делает:**

- `#ifndef/__define` - защита от двойного включения
- `extern "C"` - совместимость с C++
- Подключает всю HAL библиотеку STM32F4
- Объявляет функцию `Error_Handler()`

---

### 2️⃣ stm32f4xx_hal_conf.h - Конфигурация HAL

нужна для выбора, какие модули HAL использовать и настройки частоты
этот файл обычно создается вручную, так как он зависит от конкретного проекта и используемых периферийных устройств

**Расположение:** `include/stm32f4xx_hal_conf.h`

```c
#define HAL_MODULE_ENABLED          // Включить HAL
#define HAL_GPIO_MODULE_ENABLED     // Модуль GPIO (пины)
// `GPIO` - это модуль для работы с пинами ввода/вывода. Он позволяет настраивать пины как входы, выходы, альтернативные функции и т.д. В нашем проекте мы используем GPIO для управления встроенным LED на пине PC13, поэтому нам нужно включить этот модуль.
// `G - General Purpose Input/Output`
#define HAL_RCC_MODULE_ENABLED      // Модуль RCC (тактирование)
// `RCC` - это модуль для управления тактированием микроконтроллера. Он позволяет настраивать источники тактирования (внутренний, внешний кварц, PLL), а также делители для шин AHB, APB1 и APB2. В нашем проекте мы используем RCC для настройки частоты процессора на 96 МГц, поэтому нам нужно включить этот модуль.
// `R - Reset and Clock Control`
#define HAL_PWR_MODULE_ENABLED      // Модуль PWR (питание)
// `PWR` - это модуль для управления энергопотреблением микроконтроллера. Он позволяет настраивать режимы энергосбережения, регулировать напряжение питания и управлять функциями, связанными с питанием. В нашем проекте мы не используем функции энергосбережения, но включение этого модуля может быть полезным для будущих проектов, поэтому мы его включаем.
// `P - Power World Regulator`
#define HAL_CORTEX_MODULE_ENABLED   // Модуль процессора
// `CORTEX` - это модуль для управления функциями, связанными с ядром ARM Cortex-M4. Он включает функции для настройки приоритетов прерываний, управления системным таймером SysTick и других функций, связанных с процессором. В нашем проекте мы используем функции из этого модуля для настройки прерываний и таймера, поэтому нам нужно его включить.
#define HAL_DMA_MODULE_ENABLED      // Модуль DMA (прямой доступ к памяти)
// `DMA` - это модуль для управления прямым доступом к памяти. Он позволяет передавать данные между периферийными устройствами и памятью без участия процессора, что ускоряет работу системы и снижает нагрузку на процессор. В нашем проекте мы используем DMA для передачи данных с минимальной задержкой.
// `D - Direct Memory Access`
#define HAL_FLASH_MODULE_ENABLED    // Модуль Flash памяти
// `FLASH` - это модуль для управления встроенной Flash памятью микроконтроллера. Он включает функции для чтения, записи и стирания данных в Flash памяти. В нашем проекте мы используем Flash память для хранения программы, поэтому нам нужно включить этот модуль.
// Частота внешнего кварца
#define HSE_VALUE    25000000U      // 25 МГц на BlackPill

// Другие частоты
#define HSI_VALUE    16000000U      // Внутренний RC генератор
#define LSI_VALUE    32000U         // Низкочастотный внутренний
#define LSE_VALUE    32768U         // Низкочастотный внешний
```

**Зачем:**

- HAL - очень большая библиотека (UART, SPI, I2C, ADC, DAC, USB...)
- Здесь мы выбираем только нужные модули
- Это ускоряет компиляцию и уменьшает размер программы

---

### 3️⃣ stm32f4xx_it.c/.h - Обработчики прерываний

**Расположение:** `src/stm32f4xx_it.c` и `include/stm32f4xx_it.h`

#### Что такое прерывание (Interrupt)?

**Прерывание** - это когда что-то срочно происходит и процессор останавливает основную программу:

```diagram
Основная программа → ПРЕРЫВАНИЕ! → Обработчик → Возвращение к программе
     │                  ↑              │
     │                  │              │
     └─────────────────────────────────┘
```

**Примеры:**

- ⏰ Таймер сработал → `SysTick_Handler()`
- 🔴 Кнопка нажата → `EXTI0_IRQHandler()`
- 📡 Пришли данные UART → `USART1_IRQHandler()`
- 💥 Критическая ошибка → `HardFault_Handler()`

#### Обязательные обработчики ARM Cortex-M4

ARM Cortex-M4 это минимальный набор обработчиков, которые должны быть определены в проекте. Даже если вы не используете все эти прерывания, они должны быть присутствовать, иначе компилятор выдаст ошибку.

```c
void SysTick_Handler(void) {
    HAL_IncTick();  // Каждую 1 мс увеличивает счетчик
                    // Нужно для HAL_Delay()
}

void HardFault_Handler(void) {
    while (1) {}    // Критическая ошибка - зависание
}

void NMI_Handler(void) {}           // Non-Maskable Interrupt
void MemManage_Handler(void) {}     // Ошибка доступа к памяти
void BusFault_Handler(void) {}      // Ошибка шины данных
void UsageFault_Handler(void) {}    // Ошибка выполнения инструкции
void SVC_Handler(void) {}           // Supervisor Call (RTOS)
void DebugMon_Handler(void) {}      // Debug Monitor
void PendSV_Handler(void) {}        // Pendable Service (RTOS)
```

**Без этих обработчиков проект не скомпилируется!**

---

### 4️⃣ SystemClock_Config() - Настройка частоты

**Расположение:** `src/main.c`

#### Зачем нужно?

По умолчанию STM32F411 работает на **16 МГц** (внутренний RC генератор HSI).
Но он способен на **100 МГц**! Нам нужно его "разогнать".

Зачем это нужно?
Чем выше частота, тем быстрее работает процессор и периферия. Но это также увеличивает потребление энергии.

#### Как это работает

```diagram
[Кварц 25 МГц] → [PLL множитель] → [Делители] → [96 МГц процессор]
                                              ↓
                                         [48 МГц APB1]
                                         [96 МГц APB2]
```

#### Формула PLL (Phase-Locked Loop) для вычисления частоты процессора

```formula
f_CPU = (HSE_VALUE / PLLM) × PLLN / PLLP

f_CPU = (25 МГц / 25) × 192 / 2 = 96 МГц
```

**Параметры:**

- `PLLM = 25` - делим кварц: 25 МГц / 25 = 1 МГц (PLL - это как "посредник" между кварцем и процессором - он позволяет нам получить нужную частоту с помощью множителей и делителей)
- `PLLN = 192` - умножаем: 1 МГц × 192 = 192 МГц
- `PLLP = 2` - делим: 192 МГц / 2 = **96 МГц** 🎯
- `PLLQ = 4` - для USB: 192 МГц / 4 = 48 МГц

Аббревиатуры:

- `HSE` - High-Speed External (внешний кварц)
- `HSI` - High-Speed Internal (внутренний RC генератор)
- `PLL` - Phase-Locked Loop (множитель частоты)
- `PLLM` - PLL M (делитель для входа в PLL)
- `PLLN` - PLL N (множитель для PLL)
- `PLLP` - PLL P (делитель для процессора)
- `PLLQ` - PLL Q (делитель для USB)

#### Шины процессора

```diagram
SYSCLK (96 МГц) → Процессор, Flash
    ↓
AHB (96 МГц)    → DMA, GPIO, и т.д.
    ↓
APB1 (48 МГц)   → UART, I2C, таймеры (медленные периферийные устройства)
APB2 (96 МГц)   → SPI, ADC (быстрые периферийные устройства)
```

**Почему APB1 медленнее?**
Некоторые модули не работают на 96 МГц, поэтому их ограничивают до 48 МГц.

---

### 5️⃣ Error_Handler() - Обработка ошибок

**Расположение:** `src/main.c`

```c
void Error_Handler(void)
{
    __disable_irq();  // Отключить все прерывания
    while (1)         // Зависнуть в бесконечном цикле
    {
        // Здесь можно добавить мигание LED для индикации ошибки
    }
}
```

**Когда вызывается:**

- Ошибка настройки тактирования
- Ошибка инициализации периферии
- Любая критическая ошибка

**Что делать при ошибке:**

1. Открыть Debug режим
2. Посмотреть где программа зависла
3. Проверить настройки (частоты, пины, и т.д.)

---

## 📝 Разбор main.c

### Полный код с комментариями

```c
#include "main.h"  // Подключить HAL и объявления

void SystemClock_Config(void);  // Прототип функции

int main(void)
{
    // 1️⃣ Инициализация HAL (настройка SysTick, Flash, и т.д.)
    HAL_Init();

    // 2️⃣ Настройка частоты процессора (16 МГц → 96 МГц)
    SystemClock_Config();

    // 3️⃣ Включить тактирование GPIO Port C
    __HAL_RCC_GPIOC_CLK_ENABLE();

    // 4️⃣ Настроить пин PC13 (встроенный LED на BlackPill)
    // Структура для настройки GPIO
    // GPIO_InitTypeDef - это структура, которая содержит все настройки для одного или нескольких GPIO пинов. Она определена в HAL библиотеке и используется для конфигурации режима работы, подтяжки и скорости переключения пина.
    // GPIO_InitStruct - это переменная типа GPIO_InitTypeDef, которая будет заполнена необходимыми настройками для конкретного пина (в данном случае PC13) и передана функции HAL_GPIO_Init() для применения этих настроек к соответствующему GPIO порту.

    GPIO_InitTypeDef GPIO_InitStruct = {0};  // Структура настроек
    GPIO_InitStruct.Pin = GPIO_PIN_13;       // Пин 13
    GPIO_InitStruct.Mode = GPIO_MODE_OUTPUT_PP; // Режим: выход Push-Pull
    GPIO_InitStruct.Pull = GPIO_NOPULL;      // Без подтяжки (не подтягиваем к +3.3V и не подтягиваем к GND)
    GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_LOW; // Низкая скорость (до 8 МГц, достаточно для LED)
    HAL_GPIO_Init(GPIOC, &GPIO_InitStruct);  // Применить настройки (GPIOC - порт, &GPIO_InitStruct - указатель на структуру настроек)

    // 5️⃣ Главный цикл (выполняется бесконечно)
    while (1)
    {
        HAL_GPIO_TogglePin(GPIOC, GPIO_PIN_13); // Переключить LED
        HAL_Delay(500);                         // Задержка 500 мс
    }
}
```

### Объяснение строка за строкой

#### `HAL_Init()`

- Инициализирует HAL библиотеку
- Настраивает SysTick таймер (1 мс)
- Настраивает Flash память
- **Обязательно вызывать первой!**

#### `SystemClock_Config()`

- Настраивает PLL (Phase-Locked Loop)
- Переключает процессор с HSI (16 МГц) на PLL (96 МГц)
- Настраивает делители шин (AHB, APB1, APB2)

#### `__HAL_RCC_GPIOC_CLK_ENABLE()`

- **RCC** = Reset and Clock Control (контроллер тактирования)
- Включить тактирование для GPIO Port C
- **Без этого GPIO не работает!** (экономия энергии)

#### `GPIO_InitTypeDef`

Структура с настройками GPIO:

- `Pin` - какой пин (0-15)
- `Mode` - режим работы:
  - `GPIO_MODE_INPUT` - вход
  - `GPIO_MODE_OUTPUT_PP` - выход Push-Pull
  - `GPIO_MODE_OUTPUT_OD` - выход Open-Drain
  - `GPIO_MODE_AF_PP` - альтернативная функция
- `Pull` - подтяжка:
  - `GPIO_NOPULL` - без подтяжки
  - `GPIO_PULLUP` - к +3.3V
  - `GPIO_PULLDOWN` - к GND
- `Speed` - скорость переключения:
  - `GPIO_SPEED_FREQ_LOW` - до 8 МГц
  - `GPIO_SPEED_FREQ_MEDIUM` - до 50 МГц
  - `GPIO_SPEED_FREQ_HIGH` - до 100 МГц

#### `HAL_GPIO_TogglePin()`

- Переключает состояние пина (0 → 1 или 1 → 0)
- Для LED: включить ↔ выключить

#### `HAL_Delay(500)`

- Задержка 500 миллисекунд
- Использует SysTick таймер
- **Не точная!** (для точных задержек используйте таймеры)
- Во время задержки процессор не выполняет другие задания (блокирующая задержка)
- Для серьезных проектов лучше использовать таймеры или RTOS для неблокирующих задержек

---

## 🚀 Процесс компиляции

### Этапы сборки проекта

```diagram
1️⃣ Препроцессор (Preprocessor)
   main.c → Обработка #include, #define → main.i

2️⃣ Компилятор (Compiler)
   main.i → Проверка синтаксиса → main.s (ассемблер)

3️⃣ Ассемблер (Assembler)
   main.s → Машинный код → main.o (объектный файл)

4️⃣ Линкер (Linker)
   main.o + stm32f4xx_it.o + HAL_lib → firmware.elf

5️⃣ Конвертер (Objcopy)
   firmware.elf → firmware.bin (файл для прошивки)
```

### Результат сборки

```
Building .pio/build/blackpill_f411ce/firmware.bin
RAM:   [          ]   0.0% (used 44 bytes from 131072 bytes)
Flash: [          ]   0.7% (used 3424 bytes from 524288 bytes)
========================= [SUCCESS] =========================
```

**Что означают цифры:**

- **RAM 44 bytes** - переменные в памяти (очень мало!)
- **Flash 3424 bytes** - размер программы (~3.3 КБ)
- Еще есть **508 КБ** свободного места! 🎉

---

## 🎓 Дальнейшее обучение

### 1. Основы C

- [ ] Переменные и типы данных
- [ ] Функции
- [ ] Указатели (pointer)
- [ ] Структуры (struct)
- [ ] Препроцессор (#include, #define, #ifdef)

### 2. Основы STM32

- [ ] Архитектура ARM Cortex-M4
- [ ] Система тактирования (RCC)
- [ ] GPIO (пины ввода/вывода)
- [ ] Прерывания (NVIC)
- [ ] Таймеры
- [ ] UART (последовательный порт)

### 3. HAL библиотека

- [ ] Как работает HAL
- [ ] Инициализация периферии
- [ ] Callback функции
- [ ] DMA (прямой доступ к памяти)

### 4. Практические проекты

- [ ] ✅ Мигание LED (готово!)
- [ ] Кнопка + прерывание
- [ ] PWM (регулирование яркости LED)
- [ ] UART (связь с компьютером)
- [ ] ADC (чтение датчиков)
- [ ] I2C/SPI (связь с модулями)

---

## 📚 Полезные ресурсы

### Официальная документация ST

1. **Reference Manual** - полное описание всех регистров
   - [STM32F411 RM0383](https://www.st.com/resource/en/reference_manual/dm00119316-stm32f411xc-e-advanced-arm-based-32-bit-mcus-stmicroelectronics.pdf)

2. **HAL User Manual** - документация по HAL функциям
   - [UM1725](https://www.st.com/resource/en/user_manual/um1725-description-of-stm32f4-hal-and-lowlayer-drivers-stmicroelectronics.pdf)

3. **Datasheet** - характеристики микроконтроллера
   - [STM32F411xC/xE](https://www.st.com/resource/en/datasheet/stm32f411ce.pdf)

### YouTube каналы (на русском)

- **ITVDN** - курсы по embedded
- **Omega Lab** - STM32 туториалы
- **Mikhail Baranov** - программирование STM32

### YouTube каналы (на английском)

- **Controller Tech**
- **DigiKey**
- **Phil's Lab**

### Сообщества

- [STM32 Forum](https://community.st.com/)
- Reddit: r/stm32
- Telegram: STM32 сообщества

---

## 💡 Советы новичкам

### ✅ DO (Делайте)

- Читайте документацию
- Экспериментируйте с примерами
- Используйте debugger
- Спрашивайте в сообществах

### ❌ DON'T (Не делайте)

- Не копируйте код без понимания
- Не игнорируйте предупреждения компилятора
- Не подключайте периферию без схемы
- Не используйте задержки вместо таймеров (для серьезных проектов)

### 🔧 Инструменты

- **PlatformIO** - среда разработки
- **STM32CubeMX** - генератор кода (графическая настройка)
- **ST-Link V2** - программатор/дебагер
- **Logic Analyzer** - анализ сигналов

---

## ❓ Часто задаваемые вопросы

### Q: Почему не использовать Arduino?

**A:** STM32 мощнее (100 МГц vs 16 МГц), имеет больше периферии (UART, SPI, I2C, USB, ADC, DAC, таймеры), но сложнее в настройке.

### Q: Что лучше: HAL или Register?

**A:** Для новичков - HAL. Для оптимизации и полного контроля - регистры.

### Q: Почему проект не загружается на плату?

**A:** Проверьте:

- ST-Link подключен
- Драйверы установлены
- Плата получает питание
- Правильно выбран программатор в platformio.ini

### Q: Как отлаживать программу?

**A:** PlatformIO имеет встроенный debugger. Нажмите F5 или Debug → Start Debugging.

---

**Успехов в изучении STM32! 🚀**

*Этот файл создан как часть учебного проекта для понимания структуры STM32 проектов на PlatformIO с HAL.*
